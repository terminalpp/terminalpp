#!/bin/bash
# clone the additional repositories via https, for macOS there is no point in building the tpp-bypass target as it is only useful for WSL
git clone https://github.com/terminalpp/website
git clone https://github.com/terminalpp/ropen
git clone https://github.com/terminalpp/benchmarks
